import types

print(isinstance(types, types.ModuleType))  # Memeriksa apakah `types` adalah modul
# Fungsi: Memeriksa apakah objek adalah module.
# Kondisi: Ketika Anda ingin menjalankan verifikasi pada objek type module.